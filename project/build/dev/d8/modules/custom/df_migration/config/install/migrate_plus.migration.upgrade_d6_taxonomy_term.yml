uuid: e3139c8c-e945-4875-9b72-1872362af410
langcode: en
status: true
dependencies: {  }
id: upgrade_d6_taxonomy_term
migration_tags:
  - 'Drupal 6'
migration_group: migrate_drupal_6
label: 'Taxonomy terms'
source:
  plugin: taxonomy_term
process:
  tid:
    -
      plugin: static_map
      source: tid
      bypass: true
      map:
        83: '' # 5-man - moving to instance_size vocab
        14: '' # 10 Man - moving to instance_size vocab
        13: '' # 25 Man - moving to instance_size vocab
    -
      plugin: skip_on_empty
      method: row
  vid:
    plugin: migration
    migration: upgrade_d6_taxonomy_vocabulary
    source: vid
  name: name
  description: description
  weight: weight
  parent:
    plugin: static_map
    source: parent
    bypass: true
    map:
      92: 0 # Ulduar - move this to root since it's parent (13) is moving to new vocab
      91: 0 # Naxxramus - move this to root since it's parent (13) is moving to new vocab
      93: 0 # Sartharion - move this to root since it's parent (13) is moving to new vocab
#    -
#      plugin: migration
#      migration: upgrade_d6_taxonomy_term
#      source: parent
#    -
#      plugin: wat
  changed: timestamp
destination:
  plugin: 'entity:taxonomy_term'
migration_dependencies:
  required:
    - upgrade_d6_taxonomy_vocabulary
  optional: {  }
