<p><b>Paragraph One</b> {{ excerpt.short }}</p>
<div class="node-full__body"><p><b>Paragraph One - Full Node Body</b> {{ excerpt.short }}</p></div>
<hr>
<p><b>Paragraph Two</b> after an <code>hr</code> {{ excerpt.medium }}</p>
<div class="node-full__body"><p><b>Paragraph Two - Full Node Body</b> after an <code>hr</code> {{ excerpt.medium }}</p>
</div>
<h1>Header 1</h1>
<div class="node-full__body"><p><b>Paragraph</b> following an <code>h1</code> {{ description }}</p></div>

<h2>Header 2</h2>
<div class="node-full__body"><p><b>Paragraph</b> following an <code>h2</code> {{ description }}</p></div>

<h3>Header 3</h3>
<div class="node-full__body"><p><b>Paragraph</b> following an <code>h3</code> {{ description }}</p></div>

<h4>Header 4</h4>
<div class="node-full__body"><p><b>Paragraph</b> following an <code>h4</code> {{ description }}</p></div>

<h5>Header 5</h5>
<div class="node-full__body"><p><b>Paragraph</b> following an <code>h5</code> {{ description }}</p></div>

<h6>Header 6</h6>
<div class="node-full__body"><p><b>Paragraph</b> following an <code>h6</code> {{ description }}</p></div>

<h1>Text elements not within a node body </h1>
<p><a href="#">This is a text link.</a></p>

<p><i>Italic text is used for stylistic purposes</i> in <code>i</code></p>

<p><em>This text has added emphasis</em> in <code>em</code></p>

<p><b>Bolded text is used for stylistic purposes</b> in <code>b</code></p>

<p><strong>Strong is used to indicate strong importance</strong> in <code>strong</code></p>

<p>This text is <sup>superscript</sup></p>

<p>This text is <sub>subscript</sub></p>

<blockquote>
  <p>A block quotation (also known as a long quotation or extract) is a quotation in a written document, that is set off from the main text as a paragraph, or block of text, and typically distinguished visually using indentation and a different typeface or smaller size quotation.</p>
</blockquote>


<h1>Text elements within a node body</h1>
<div class="node-full__body">
  <p><a href="#">This is a text link.</a></p>

  <p><i>Italic text is used for stylistic purposes</i> in <code>i</code></p>

  <p><em>This text has added emphasis</em> in <code>em</code></p>

  <p><b>Bolded text is used for stylistic purposes</b> in <code>b</code></p>

  <p><strong>Strong is used to indicate strong importance</strong> in <code>strong</code></p>

  <p>This text is <sup>superscript</sup></p>

  <p>This text is <sub>subscript</sub></p>

  <blockquote>
    <p>A block quotation (also known as a long quotation or extract) is a quotation in a written document, that is set off from the main text as a paragraph, or block of text, and typically distinguished visually using indentation and a different typeface or smaller size quotation.</p>
  </blockquote>
</div>

